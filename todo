todos
+ protect actions than cannot be done while synchronizing
+ use ZASync to deal with concurrent synchronizations
+ add security
+ translations in english
+ set some options in mailtool by manager that can't be changed by user
+ recreate attache part in fwd messages
+ add a star in front of folder that contains "new" messages
+ make folder that contains "unread" messages
+ define several synchronization strategies depending on users use
+ filesystem storages
+ see why SSL won't work on some servers like bertha

cosmetics

+ review CSS naming conventions
+ review all html code style

later

+ entring message filtering
+ spell checker
+ scan mail body to generate contacts
+ anti spam
+ anti virus
+ dates index in catalog
+ full mail specific UI for address books
+ add options for sanitizing : keep or delete links, images in bodies [Show Image button]
